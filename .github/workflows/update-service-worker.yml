name: Update Cache Version

on:
  push:
    branches:
      - main  # Executa apenas na branch `main`

jobs:
  update-cache:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3

      - name: Garantir que a branch esteja atualizada
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase || echo "Nenhuma atualização necessária"

      - name: Gerar novo número de versão baseado no timestamp
        run: |
          CACHE_VERSION=$(date +%s)
          echo "CACHE_VERSION=$CACHE_VERSION" >> $GITHUB_ENV
          echo "Nova versão gerada: $CACHE_VERSION"

      - name: Substituir placeholder no `service-worker.js`
        run: |
          sed -i "s/__CACHE_VERSION__/${{ env.CACHE_VERSION }}/g" service-worker.js
          echo "Substituição concluída para a versão ${{ env.CACHE_VERSION }}"

      - name: Atualizar `version.txt` para forçar atualização do cache
        run: |
          echo "${{ env.CACHE_VERSION }}" > version.txt
          echo "Arquivo version.txt atualizado para ${{ env.CACHE_VERSION }}"

      - name: Commit das alterações e push (se houver mudanças)
        run: |
          git add service-worker.js version.txt
          
          if git diff --cached --quiet; then
            echo "Nenhuma alteração para commit."
          else
            git commit -m "🔄 Atualiza CACHE_VERSION para ${{ env.CACHE_VERSION }}"
            git push
          fi
