name: Update Cache Version

on:
  push:
    branches:
      - main  # Define a branch que ativa a atualização

jobs:
  update-cache:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3

      - name: Gera um novo número de versão baseado no timestamp
        run: |
          CACHE_VERSION=$(date +%s)
          echo "CACHE_VERSION=$CACHE_VERSION" >> $GITHUB_ENV
          echo "Versão gerada: $CACHE_VERSION"  # Apenas para debug

      - name: Substitui o placeholder no ficheiro JavaScript
        run: |
          sed -i "s/__CACHE_VERSION__/${{ env.CACHE_VERSION }}/g" service-worker.js
          echo "Substituição concluída para versão ${{ env.CACHE_VERSION }}."

      - name: Commit das alterações e push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add service-worker.js
          git commit -m "🔄 Atualiza CACHE_VERSION para ${{ env.CACHE_VERSION }}" || echo "Nenhuma alteração para commitar"
          git pull --rebase || echo "Nenhuma atualização necessária"
          git push || echo "Nada para atualizar"
